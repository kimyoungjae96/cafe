import React from 'react';
import { View } from '@/components';

const Divider = () => {
  return (
    <View style={{ height: 6, backgroundColor: '#EFEFEF', width: '100%' }} />
  );
};

export default Divider;
